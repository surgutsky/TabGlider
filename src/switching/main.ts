// Switching page — pure HTML/CSS spinner, no Svelte needed.
// The background service worker opens this page as an anchor tab while
// closing old tabs and opening new ones, then closes this tab itself.
export {}
